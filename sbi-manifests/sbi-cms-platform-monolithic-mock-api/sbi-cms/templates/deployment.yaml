---
# Source: sbi-cms/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sbi-cms-platform-monolithic-mock-api
  namespace: sbi
  labels:
    app: sbi-cms-platform-monolithic-mock-api
    intent: apps
spec:
  selector:
    matchLabels:
      app: sbi-cms-platform-monolithic-mock-api
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: sbi-cms-platform-monolithic-mock-api
        intent: apps
      annotations:
        kubernetes.io/psp: eks.privileged
        "fss.com/app-configmap-hash": 74782a1a8b702078fd4103f0e4b14f2d132cfdf81d2d835f98d5751feaeb20af
    spec:
      serviceAccountName: sbi-cms-platform-monolithic-mock-api-sa
      volumes:
        - name: key-cert-vol
          configMap:
            name: public-private-key
        - name: visa-npci-key-configmap-volume
          configMap:
            name: visa-npci-key-configmap
        - name: dcms-keys-volume
          configMap:
            name: dcms-keys-configmap
        - name: sbi-keys-volume
          configMap:
            name: sbi-keys-configmap
      containers:
        - name: sbi-cms-platform-monolithic-mock-api
          image: 736483234466.dkr.ecr.ap-south-1.amazonaws.com/sbi-cms-platform-monolithic-mock-api:sbi-uat.v56
          volumeMounts:
            - name: key-cert-vol
              mountPath: /config
            - name: visa-npci-key-configmap-volume
              mountPath: /data
            - name: dcms-keys-volume
              mountPath: /vol
            - name: sbi-keys-volume
              mountPath: /sbi
          ports:
            - containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: sbi-cms-platform-monolithic-mock-api
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          imagePullPolicy: IfNotPresent
      nodeSelector:
        app: sbi
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      securityContext: {}
      schedulerName: default-scheduler
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600
